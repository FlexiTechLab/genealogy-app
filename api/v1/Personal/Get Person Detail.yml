info:
  name: Get Person Detail
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/persons/4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
