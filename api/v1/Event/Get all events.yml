info:
  name: Get all events
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/trees/:tree_id/events"
  params:
    - name: tree_id
      value: 019d2848-5228-7551-934d-934dae4131fa
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
